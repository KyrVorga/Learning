// create a function that takes in a grid
// size and returns a 2d array of that size
// [
//     [1,2,3],
//     [4,5,6],
//     [7,8,9]
// ]

// const createGrid = (dimensions) => {
// 	const [x, y] = dimensions;
// 	const grid = new Array(x);

// 	grid.fill([], 0, x);
// 	grid.forEach((elem, i) => {
// 		elem.push(i);
// 	});

// 	return grid;
// };

// console.log(createGrid([5, 5]));

function sum(a, b) {
	return a + b;
}
